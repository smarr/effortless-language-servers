language: java
jdk:
  - oraclejdk8
sudo: false
before_install:
  - nvm install 8
script:
  - npm install .
  - npm run compile
  # - if [ "$TASK" = "checkstyle" ]; then ant checkstyle-jar && ant checkstyle; fi
